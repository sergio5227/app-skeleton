import { StyleSheet } from "react-native";

const misBonsaisStyle = StyleSheet.create({
    content: {
        flex: 1,
        marginTop: 50,
        paddingLeft: 10,
        paddingRight: 10
    }
})
export default misBonsaisStyle;